{{# categories }}
<div class="sidebar-item categories row">

	<div class="title">
	{{# title }}
			{{. | ucfirst}} {{# withTotal }}({{content | count }}){{/ withTotal }}
		{{/ title }}
		{{^ title }}
			{{#transu}}categories{{/transu}} {{# withTotal }}({{content | count }}){{/ withTotal }}
		{{/ title }}
	</div>

	<ul class="list-group">
		{{# content }}
			<a href="/{{categoryBase}}/{{getSlug}}"><li class="list-group-item">{{getName}}</li></a>
		{{/ content }}
	</ul>

</div>
{{/ categories  }}